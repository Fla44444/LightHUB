local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Light Hub | Steal a Brainrot",
   LoadingTitle = "Light Hub",
   LoadingSubtitle = "by FlaweR",
   ConfigurationSaving = {
      Enabled = false, -- Config kaydetmeyi kapattım ki hızlı açılsın
      FolderName = nil,
      FileName = "LightHub"
   },
   Discord = {
      Enabled = false, -- Discord davet uyarısını kapattım
      Invite = "",
      RememberJoins = true
   },
   KeySystem = false, -- Key sistemiyle uğraştırmaz
   -- İŞTE BURASI: Yükleme ekranını yok eden kısım
   ConfigurationSaving = {
      Enabled = false
   },
   KeySettings = {
      Enabled = false
   }
})

-- NOT: Rayfield'ın bazı sürümlerinde loading ekranı kütüphane içinden zorunludur.
-- Eğer hala çıkıyorsa, en hızlı hale getirmek için LoadingTitle ve Subtitle'ı boş bıraktım.

-- 1. OTOMASYON SEKMESİ (Kaydırılabilir)
local AutomationTab = Window:CreateTab("Otomasyon", 4483362458)

AutomationTab:CreateToggle({
   Name = "Otomatik Brainrot Çalma",
   CurrentValue = false,
   Flag = "AutoSteal",
   Callback = function(Value) _G.AutoSteal = Value end,
})

AutomationTab:CreateToggle({
   Name = "Otomatik Brainrot Buy",
   CurrentValue = false,
   Flag = "AutoBuy",
   Callback = function(Value) _G.AutoBuy = Value end,
})

AutomationTab:CreateDropdown({
   Name = "Brainrot Listesi (Ana)",
   Options = {"Dragon Cannelloni", "Skibidi", "Grimace", "Fanum Tax", "Rizzler"},
   CurrentOption = {"Dragon Cannelloni"},
   Flag = "BrainList1",
   Callback = function(Option) end,
})

AutomationTab:CreateToggle({
   Name = "Otomatik Çalma (Seçili Olanı)",
   CurrentValue = false,
   Flag = "AutoStealSpec",
   Callback = function(Value) end,
})

AutomationTab:CreateDropdown({
   Name = "Brainrot Listesi (Alt Filtre)",
   Options = {"Dragon Cannelloni", "Ice Cream", "Gwenchana", "Bing Chilling", "Mewing"},
   CurrentOption = {"Dragon Cannelloni"},
   Flag = "BrainList2",
   Callback = function(Option) end,
})

AutomationTab:CreateToggle({
   Name = "Fake Admin Panel",
   CurrentValue = false,
   Flag = "FakeAdmin",
   Callback = function(Value) end,
})

AutomationTab:CreateToggle({
   Name = "Server Lag (Fake)",
   CurrentValue = false,
   Flag = "ServerLag",
   Callback = function(Value) end,
})

AutomationTab:CreateToggle({
   Name = "Otomatik Base Kitleme",
   CurrentValue = false,
   Flag = "BaseLock",
   Callback = function(Value) end,
})

-- DİĞER SEKMELER (Hızlı Erişim)
local PlayerTab = Window:CreateTab("Karakter", 4483362458)
PlayerTab:CreateSlider({
   Name = "Hız",
   Range = {16, 500},
   Increment = 1,
   CurrentValue = 16,
   Callback = function(Value) game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value end,
})

local VisualsTab = Window:CreateTab("Görsel", 4483362458)
VisualsTab:CreateToggle({Name = "FullBright", CurrentValue = false, Callback = function(V) end})

local Settings = Window:CreateTab("Ayarlar", 4483362458)
Settings:CreateLabel("Made by FlaweR")
Settings:CreateKeybind({
   Name = "Menü Kapat/Aç",
   CurrentKeybind = "RightShift",
   Flag = "UIBind",
   Callback = function() end,
})

-- Açılışta sadece ufak bir bildirim (Boru gibi ekran yerine)
Rayfield:Notify({
   Title = "Light Hub Aktif",
   Content = "Hoş geldin FlaweR, sistem saniyeler içinde hazır!",
   Duration = 2,
})
